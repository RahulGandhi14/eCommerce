const authRequests = {
    loginUser: "/auth/login",
    register: "/auth/userRegister",
}

const userDetailRequests = {
    address: "/address",
}

const productRequests = {
    product: "/product",
}

module.exports = {
    authRequests,
    userDetailRequests,
    productRequests,
}