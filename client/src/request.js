const authRequests = {
    loginUser: "/auth/login",
    register: "/auth/userRegister",
}

const userDetailRequests = {
    address: "/address",
}

module.exports = {
    authRequests,
    userDetailRequests
}